import type { FloorPlan } from "@/types/floor-plan";
import sampleJson from "../../samples/2bhk-flat.json";

/** Premium 2BHK test plan — edit `samples/2bhk-flat.json` directly. */
export const sampleFloorPlan = sampleJson as unknown as FloorPlan;
